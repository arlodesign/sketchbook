window.sketchHeadline = "";

function setup() {
  pixelDensity(4);
  const myCanvas = createCanvas(660, 840);
  myCanvas.parent('my-canvas');
  // SETUP
}

function draw() {
  background('white');
  // DRAW
}
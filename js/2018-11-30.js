const scalar=9;let diagonal,gridWidth,gridHeight;function getHypotenuse(t,e){return Math.sqrt(t*t+e*e)}function getDistanceFromPointer(t,e){return getHypotenuse(Math.abs(mouseX-t),Math.abs(mouseY-e))}function setup(){pixelDensity(4),createCanvas(660,840).parent("my-canvas"),rectMode(CENTER),noFill(),diagonal=getHypotenuse(width,height),gridWidth=width/scalar,gridHeight=height/scalar}function draw(){background("white");for(let t=-Math.floor(gridHeight/2);t<Math.ceil(gridHeight/2);t++)for(let e=-Math.floor(gridWidth/2);e<Math.ceil(gridWidth/2);e++){const i=width/2+3*scalar*e,a=height/2+3*scalar*t,r=getDistanceFromPointer(i,a),o=map(r,0,diagonal,10*scalar,3*scalar);strokeWeight(map(r,0,diagonal,2,0)),rect(i,a,o,o)}first=!1}function touchMoved(){if(mouseX>=0&&mouseY>=0&&mouseX<=width&&mouseY<=height)return!1}setTimeout(()=>{drawingComplete=!0},1e3);
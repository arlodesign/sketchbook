<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= siteName %></title>
    <link rel="prefetch" href="<%= domain %>/<%= latestSketch.url %>" />
    <link rel="canonical" href="<%= domain %>/" />
    <meta name="description" content="Experiments with generative art" />
    <meta property="og:description" content="Experiments with generative art" />
    <meta property="og:type" content="website" />
    <meta
      property="og:image"
      content="<%= domain %>/<%= latestSketch.url %>facebook.png"
    />
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:creator" content="@arlodesign" />
    <meta
      name="twitter:description"
      content="Experiments with generative art"
    />
    <meta
      property="twitter:image"
      content="<%= domain %>/<%= latestSketch.url %>twitter.png"
    />
    <style>
      body {
        background: #111;
        color: #eee;
      }
      header,
      nav,
      main {
        opacity: 0;
      }
    </style>
  </head>
  <body>
    <noscript>
      <p>This site requires Javascript to view artwork.</p>
    </noscript>
    <header>
      <h1><%= siteName %></h1>
      <nav>
        <ul>
          <li>
            <a class="nav--link" href="#about">
              <span>About</span>
              <svg
                class="icon"
                xmlns="http://www.w3.org/2000/svg"
                viewBox="0 0 24 24"
              >
                <circle
                  fill="none"
                  stroke="currentColor"
                  class="cls-1"
                  cx="12"
                  cy="12"
                  r="7.5"
                />
                <path
                  fill="currentColor"
                  d="M13 15v-5h-3v1h1v4h-1v1h4v-1h-1z"
                />
                <circle fill="currentColor" cx="12" cy="8" r="1.25" />
              </svg>
            </a>
          </li>
        </ul>
      </nav>
    </header>
    <main id="main">
      <ul>
        <% sketches.forEach(function(sketch, i) { %>
        <li id="sketch-<%= sketch.title %>">
          <a href="<%= sketch.url %>">
            <figure>
              <% if (prod) { %>
              <img
                src="<%= sketch.url %>thumb@1x.png"
                srcset="<%= sketch.url %>thumb@1x.png 1x, <%= sketch.url %>thumb@2x.png 2x"
              />
              <% } %>  <% if (!prod) { %>
              <img src="<%= sketch.url %>thumb.png" />
              <% } %>
              <figcaption>
                <time datetime="<%= sketch.title %>"> <%= sketch.title %></time>
              </figcaption>
            </figure>
          </a>
        </li>
        <% }); %>
      </ul>
    </main>
    <footer id="about">
      <h2>About</h2>
      <p>
        This site presents semi-daily explorations of generative and procedural
        art. The code is all available on
        <a href="https://github.com/arlodesign/sketchbook/">GitHub</a>.
      </p>
      <p>
        <a href="https://arlo.me/">Arlo Bryan Guthrie</a>
        often uses this work to produce screenprints and other printed artwork.
        He might even start selling it some day. He’s on
        <a href="https://www.instagram.com/arlodesign/">Instagram</a>.
      </p>
      <a class="footer--close" href="#main">
        <svg
          class="icon"
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
        >
          <g fill="none" stroke="currentColor">
            <path d="M18 6 6 18M18 18 6 6" />
          </g>
        </svg>
        <span>Close</span>
      </a>
    </footer>
  </body>
</html>
